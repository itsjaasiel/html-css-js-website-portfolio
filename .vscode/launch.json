{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Web Server",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:5500",
            "webRoot": "${workspaceFolder}"  // The root directory of your project
        }
    ]
}